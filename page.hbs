{{!

    PAGE TEMPLATE
    ! Configuration of this file is optional.

}}
{{!< default}}

{{#post}}

  {{> post_feature_header}}

  <div class="container {{post_class}}">
    <div class="customcontainer">
      {{> sidebar_post}}
      <div class="content">
        {{! WIDGET - RESPONSIVE BLOG INFO }}
        <div class="widget postinfo_widget inline {{post_class}}">
          {{#if @blog.logo}}
            <a href="{{@blog.url}}" class="logo"><img src="{{@blog.logo}}" alt="{{@blog.title}}"></a>
          {{/if}}
        </div>

        <div class="postlist">

          {{! POST MAIN }}
          <article class="post standard single">
            {{> post_header}}
            <div class="postcontents">
              {{content}}
            </div>
          </article>

          {{! WIDGET - AUTHOR INFO }}
          {{> widget_author}}

          {{> responsive_nav}}
        </div>
      </div>
    </div>
  </div>

{{/post}}
